{
  "name": "asc-tea-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Root dev-orchestration for the ASC Tea Auction platform (frontend + backend run separately as their own projects).",
  "scripts": {
    "dev": "npm run db:start && concurrently -n API,WEB -c blue,green \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "cross-env DOTNET_gcServer=0 DOTNET_GCHeapHardLimit=0x40000000 dotnet run --project backend/Asc.Api --urls http://0.0.0.0:5058",
    "dev:frontend": "cross-env NODE_OPTIONS=--max-old-space-size=1536 npm run dev --prefix frontend",
    "db:start": "powershell -NoProfile -ExecutionPolicy Bypass -File data/mongodb/start-mongodb.ps1"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^10.1.0"
  }
}
