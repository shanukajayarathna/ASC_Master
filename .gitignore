# Backend (.NET)
backend/**/bin/
backend/**/obj/
backend/.config/

# Frontend (Next.js) — has its own .gitignore too, this covers repo-root tooling
frontend/node_modules/
frontend/.next/
frontend/.env.local

# Root dev-orchestration (concurrently)
node_modules/

# docker-compose secrets — see .env.example for the template
/.env

# Real weekly-sale Excel files (confidential market data, ~35MB each) — local import
# inputs for POST /api/dev/import-sales, never committed. Anchored to the repo root so
# it can't swallow source folders like backend/Asc.Api/Data.
/data/

# OS
.DS_Store
Thumbs.db

# Unrelated local scratch content, not part of the ASC product
/Rishan IT/
